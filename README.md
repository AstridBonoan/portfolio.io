# Portfolio (Vite + React + Tailwind)

This project is scaffolded with Vite, React and Tailwind CSS.

Quick start:

```bash
npm install
npm run dev
```

Tailwind is configured via `tailwind.config.js` and `postcss.config.cjs`.
